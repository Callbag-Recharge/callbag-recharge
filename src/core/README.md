# Core

Foundation of callbag-recharge. Six primitives (`producer`, `state`, `derived`, `dynamicDerived`, `operator`, `effect`) plus protocol constants, types, inspector, batch, and pipe.

Everything in this folder is the bedrock — other folders build on it. Core never imports from any other folder.
