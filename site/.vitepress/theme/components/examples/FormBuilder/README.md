# D2: Form Builder

Multi-field form with sync + async validation, dirty/pristine tracking, derived aggregation.

## Files (planned)

- `store.ts` — `formField`, `derived`, `debounce`, `combine`, `state`
- `FormBuilder.vue` — form UI with validation indicators, source panel highlighting `formField` wiring
