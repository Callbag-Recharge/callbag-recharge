# D3: Agent Loop

Tool call cycle: plan, execute, observe, decide — with approval gate for human-in-the-loop.

## Files (planned)

- `store.ts` — `agentLoop`, `gate`, `approval`, `stateMachine`, `track`
- `AgentLoop.vue` — step visualization, approval UI, source panel highlighting the cycle
