# D5: State Machine Visualizer

Interactive FSM with typed transitions, visual state graph.

## Files (planned)

- `store.ts` — `stateMachine`, `state`, `derived`
- `StateMachine.vue` — state graph visualization, transition buttons, source panel highlighting FSM definition
