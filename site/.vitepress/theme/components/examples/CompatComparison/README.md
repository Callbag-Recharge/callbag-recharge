# D6: Compat Comparison

Same counter/todo app implemented in callbag-recharge vs Jotai vs Zustand vs TC39 Signals — side by side.

## Files (planned)

- `store.ts` — `state`, `derived`, `effect` + compat layers
- `CompatComparison.vue` — tabbed or side-by-side view, source panel showing each implementation
