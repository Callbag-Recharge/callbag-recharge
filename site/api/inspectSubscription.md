# inspectSubscription()

Inspect a subscription's current state.

## Signature

```ts
function inspectSubscription(sub: TopicSubscription<any>): SubscriptionInfo
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `sub` | `TopicSubscription&lt;any&gt;` | The subscription to inspect. |

## Returns

`SubscriptionInfo` snapshot.
